/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
    images: {
    domains: [
      'images.unsplash.com',
      "i.ibb.co"
    ],
  },
  reactCompiler: true,
};

export default nextConfig;
